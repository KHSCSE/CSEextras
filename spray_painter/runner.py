import pygame, random, math

# setup
w = 800
h = 600
screen = pygame.display.set_mode([w, h])
screen.fill((255, 255, 255))

# variables for circle size, number of specks, color




# stay on screen
running = True
while running:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False

    # check if button is pressed, change color
    
    
    # check if mouse is pressed, spray!!



   
    
    
    # pushes the screen to the current display
    pygame.display.flip()
    
    # delay a moment
    pygame.time.delay(50)


